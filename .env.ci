# CI-specific environment variables
# No secrets - these are injected via CI environment variables
# Use override=false when loading so injected values remain authoritative

# CI-specific runtime settings
NODE_ENV=test
LOG_LEVEL=error
DISABLE_COLORS=true

# CI-specific timeouts (more conservative)
REQUEST_TIMEOUT_MS=5000
RETRY_MAX_ATTEMPTS=2
RETRY_BASE_DELAY_MS=100

# Test database override (will use injected DATABASE_URL in production)
# DATABASE_URL=postgres://test:test@localhost:5432/civicue_test